# Vita Health App Environment Variables
# Firebase configuration is included in the build
# No environment variables needed for production deployment

# Optional: Development settings
# REACT_APP_ENV=development
# REACT_APP_DEBUG=true

# Firebase configuration is in src/lib/firebase.js
# Update that file with your Firebase project credentials
